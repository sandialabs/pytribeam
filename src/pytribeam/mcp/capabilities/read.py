#!/usr/bin/python3
"""
Read Capabilities
=================

Capabilities that observe the microscope without commanding it.

Note that "read-only" here means "issues no commands", not "has no effect".
Capturing a full state sweeps the imaging quadrants and can close an open menu
in the vendor UI, so these are annotated honestly rather than advertised as
free.

Work package 4.
"""
